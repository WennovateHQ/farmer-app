import 'package:flutter/material.dart';

// This is a placeholder dashboard screen to resolve the import error
// The actual dashboard implementation is in main.dart
class DashboardScreen extends StatelessWidget {
  const DashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // Just return an empty container - actual implementation is in main.dart
    return Container();
  }
}
